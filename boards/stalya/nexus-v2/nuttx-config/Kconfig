#
# For a description of the syntax of this configuration file,
# see misc/tools/kconfig-language.txt.
#
# nexus-v2 has no board options of its own: the firmware is loaded into DDR
# by Linux and everything it needs is selected in the chip and PX4 configs.
#
